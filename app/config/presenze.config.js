/* ============================================================
 * presenze.config.js — Configurazione presenze
 * Sostituire questo file per modificare giorni e opzioni extra.
 * ============================================================ */

window.CAMP_PRESENZE_CONFIG = {
  dayNames: ['Lun', 'Mar', 'Mer', 'Gio', 'Ven', 'Sab'],
  dayCount: 6,
  extraOptions: []
};
